# Gallery SaaS — онлайн-галереї для фотографів (MVP, Phase 1)

Платформа для фотографів: галереї для передачі зйомок клієнтам (Phase 1),
конструктор сайтів (Phase 2), профіль-портфоліо (Phase 3). Модель продукту —
Pixover, естетика — Vigbo.

**Ключовий архітектурний принцип:** це egress-важкий медіа-продукт. Усе медіа
живе в Cloudflare R2 (нульовий egress) і передається напряму між браузером і
сховищем через presigned URLs. Supabase — тільки дані та авторизація. Vercel
ніколи не проксіює байти фотографій.

## Стек

- Next.js 14 (App Router), TypeScript strict, React Server Components
- Tailwind CSS + design tokens на CSS-змінних (`src/app/globals.css`)
- Supabase (Postgres + Auth + RLS) — дані й авторизація, НЕ медіа
- Cloudflare R2 за абстракцією `src/lib/storage/` (`StorageProvider` → `R2Provider`)
- i18n: `uk` (за замовчуванням) + `en`, роутинг `/uk/...`, `/en/...`

## Що вже працює (Phase 1, перший зріз)

- Вхід через magic link (Supabase Auth), автостворення профілю тригером
- Кабінет: список галерей, використання сховища, створення галереї
  (назва, опис, дата, пароль, термін дії)
- Завантаження фото/відео: браузер → R2 напряму через presigned PUT,
  паралельні аплоади з прогресом, перевірка квоти сховища
- Великі відео (понад 64 МБ) — через S3 multipart: частини по 16 МБ
  паралельно, ретраї з експоненційним бекофом на кожну частину окремо,
  abort-очищення при провалі; обрив мережі більше не скидає завантаження
  на нуль
- Варіанти зображень (preview 2048px / thumb 512px) генеруються в браузері
  при завантаженні (`src/lib/images/variants.ts`) і йдуть тим самим
  presigned-шляхом; у перегляді галереї оригінал ніколи не віддається
- Публічна галерея за slug: RLS відкриває лише опубліковані й не прострочені
  галереї; опційний пароль через HMAC-cookie; лічильник переглядів
- Відбір фото клієнтом (favorites) без реєстрації — анонімний токен
  у httpOnly-cookie, оптимістичний toggle, лічильники в кабінеті
- «Завантажити оригінал» — redirect на короткоживучий presigned GET + подія
  `download` у статистиці
- «Завантажити всі фото архівом» — браузер тягне оригінали напряму з R2 і
  збирає zip локально (fflate, store-режим) — нуль egress через сервер.
  У Chromium архів стрімиться одразу на диск (File System Access API),
  тож навіть багатогігабайтна галерея не тримається в пам'яті; в інших
  браузерах — fallback через Blob
- Статистика в кабінеті: перегляди, завантаження, обране по кожному фото
- Розділ «Обране клієнта» на сторінці галереї: сітка обраних фото,
  кількість клієнтів, експорт списку імен файлів (буфер + .txt) для ретуші
- Брендинг: ім'я та лого фотографа в налаштуваннях профілю показуються
  вгорі публічної галереї (RPC `get_gallery_branding` віддає лише ці два
  поля і лише через опубліковану галерею)
- Оплати за абстракцією `src/lib/payments/` (LiqPay для MVP): тарифи
  free 3 ГБ / start 100 ГБ / pro 500 ГБ у `src/lib/plans.ts`, сторінка
  тарифів з перемикачем місяць/рік, checkout через hosted-форму LiqPay,
  webhook з перевіркою підпису оновлює план і ліміт сховища. Без ключів
  LiqPay UI чемно повідомляє, що оплати ще не підключені
- Видалення галереї: спочатку об'єкти в R2, потім рядок у БД
  (тригер повертає квоту)
- Бронювання зйомок (`/uk/b/<handle>`): фотограф вмикає запис, додає слоти
  (дата, час, тривалість, ціна) і способи оплати — Monobank-еквайринг чи
  WayForPay на ВЛАСНИХ токенах (гроші йдуть напряму фотографу, платформа
  не посередник), ручне посилання або реквізити текстом. Клієнт обирає слот,
  лишає обов'язкові контакти; бронь race-safe (`free → booked`, двоє одночасно
  не займуть один час) і не залежить від оплати. Вебхуки не довіряють тілу
  запиту — сервер перезапитує статус у банку токеном фотографа, і лише
  реальний success ставить `paid` (ідемпотентно). Ручні оплати фотограф
  підтверджує в кабінеті; кнопки «я оплатив» у клієнта немає. Токени ніколи
  не потрапляють у браузер. Сповіщення про нову бронь — на пошту
  (Brevo, опційно)
- Автоматизація бронювання: неоплачений слот сам звільняється через N годин
  (налаштовується, 0 вимикає; спрацьовує ліниво при кожному показі слотів —
  без cron), а ручні оплати можна підтверджувати автоматично звіркою з
  випискою Monobank (персональний токен фотографа, ліміт 1 запит/хв,
  збіг за сумою після часу броні; кожне надходження закриває лише одну бронь)
- Конструктор сайтів (Phase 2, перший зріз): публічний сайт `/uk/s/<handle>`
  зі спільних блоків (hero, портфоліо, про мене, прайс, контакти) і темами
  як наборами токенів (`src/lib/site/themes.ts`) — вісім тем з каталогу,
  «Опівніч» = нічна інверсія «Тиші» одним перемикачем. Портфоліо збирається
  автоматично з опублікованих галерей. Редактор у кабінеті, жодного
  брендингу платформи на сайті

## Локальний запуск

```bash
cp .env.example .env.local   # заповнити значення
npm install
npm run dev
```

### 1. Supabase

Створіть проєкт, застосуйте міграцію:

```bash
npx supabase link --project-ref YOUR_PROJECT
npx supabase db push          # застосує supabase/migrations/0001_phase1_galleries.sql
```

В Auth → URL Configuration додайте `http://localhost:3000/auth/callback`
(і продакшн-URL) до Redirect URLs.

### 2. Cloudflare R2

1. Створіть bucket (наприклад `gallery-media`) і API token з правами
   Object Read & Write на цей bucket.
2. Додайте CORS-правило на bucket (браузер робить PUT/GET напряму):

```json
[
  {
    "AllowedOrigins": ["http://localhost:3000", "https://YOUR_APP_DOMAIN"],
    "AllowedMethods": ["PUT", "GET"],
    "AllowedHeaders": ["content-type"],
    "ExposeHeaders": ["etag"],
    "MaxAgeSeconds": 3600
  }
]
```

`ExposeHeaders: ["etag"]` обов'язковий: multipart-завантаження великих відео
читає ETag кожної частини з відповіді R2, без нього браузер не зможе
завершити збірку файла.

3. Заповніть `R2_*` змінні в `.env.local`.

## Схема даних (Phase 1)

`supabase/migrations/0001_phase1_galleries.sql`:

| Таблиця | Призначення |
|---|---|
| `profiles` | Фотограф: план, ліміт/використання сховища (рахується тригерами) |
| `galleries` | Галерея: slug, пароль (scrypt-hash), термін дії, публікація, перегляди |
| `assets` | Метадані фото/відео; сам файл — в R2 за `r2_key`; `variants` — майбутні thumbnail/preview |
| `selections` | Відбір клієнта (favorite/retouch) за анонімним `client_token` |
| `gallery_events` | Події view/download для статистики |
| `payments` | Історія оплат; пишеться лише сервером (service role), власник читає своє |

Уся публічна видимість — через RLS: анонім бачить лише опубліковану,
не прострочену галерею та її асети. Пароль перевіряється в app-шарі
(HMAC-cookie `g_unlock_*`), хеш ніколи не потрапляє в props сторінки.

## Потік завантаження (критичний шлях)

```
браузер ── POST /api/uploads/presign ──► перевірка власника + квоти → presigned PUT URL
браузер ── PUT (файл) ────────────────► R2 напряму (XHR, прогрес)
браузер ── POST /api/uploads/complete ► рядок в assets (RLS), тригер оновлює квоту
```

Перегляд: сервер підписує короткоживучі GET URL (preview або оригінал,
поки немає пайплайна варіантів) — браузер тягне медіа напряму з R2.

## Тарифи (v2)

Єдине джерело цін і фіч — `src/lib/plans.ts` (лендинг, білінг, checkout і
гейти читають звідти). Галереї: free 3 ГБ / Базовий 50 ГБ 149₴ / Плюс 200 ГБ
369₴ / Про 500 ГБ 599₴ / Максимальний 1 ТБ 999₴ / Максимальний+ 2 ТБ 1699₴;
річна = два місяці в подарунок. Фіче-гейти: зняття брендингу і лого — з
Базового; відео, статистика, чайові — з Плюса; пріоритетна підтримка — з Про.
Сайти окремо: Пробний 0 / Базовий 99₴ / Плюс 149₴ (бандл із галереями −15% —
поки лише маркетинговий намір). Скасування підписки → grace-період 7 днів,
далі ліміт поводиться як безкоштовний (ліниве застосування при завантаженні,
файли не видаляються). Легкі тіри пораховані під R2, важкі (від 500 ГБ) — під
B2: перенесення — це один новий клас `StorageProvider`.

## Наступні кроки (у порядку)

1. Watermark-опція для preview + серверний пайплайн варіантів
   (замінить браузерну генерацію; точка підключення CDN —
   `src/lib/images/loader.ts`)
2. Відновлення multipart-завантаження після перезавантаження сторінки
   (зараз ретраї працюють у межах сесії; потрібна персистенція
   uploadId + частин у localStorage)
3. Ретеншн після скасування підписки: grace-період замість миттєвого
   даунґрейду ліміту (зараз webhook одразу знижує ліміт, файли не чіпає)
4. Phase 2 — конструктор сайтів; Phase 3 — профіль-портфоліо
