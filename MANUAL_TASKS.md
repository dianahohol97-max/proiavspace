# Що я маю зробити вручну (станом на 24.07.2026)

Увесь код готовий і задеплоєний. Тут зібрані **тільки ті кроки, які можу
зробити лише я** — бо вони потребують моїх акаунтів, карток чи платних
планів. Порядок — за пріоритетом. Нічого з цього не блокує роботу сайту
прямо зараз; це вмикає додаткові можливості.

---

## 🟢 Швидкі (5–15 хв кожне)

### 1. Запустити блог-движок (автоматичні SEO-статті)
> **Оновлення:** блог уже НЕ порожній — я написала й опублікувала **8 статей**
> напряму в базу (разом із 6 вбудованими це **14 живих статей** на
> proiav.space/uk/blog). Тобто контент уже працює на SEO. Цей крок тепер
> потрібен лише щоб статті **дописувалися самі** далі (у черзі ще 84 теми).
> Не терміново.

Движок написаний, черга — 84 теми. Треба лише дати йому ключі.

1. GitHub → репозиторій → **Settings → Secrets and variables → Actions → New repository secret**. Додати три:
   - `GEMINI_API_KEY` — з [aistudio.google.com](https://aistudio.google.com) (Get API key, безкоштовно).
   - `SUPABASE_URL` — `https://otquxwszyriwlfqfdusg.supabase.co`
   - `SUPABASE_SERVICE_ROLE_KEY` — Supabase → Settings → API → service_role (секрет!).
2. Перевірити вручну: **Actions → «Blog — generate article» → Run workflow**.
3. Далі саме працює за розкладом (пн/чт). Чернетки перевіряю в кабінеті:
   **Кабінет → Блог** — там читаю, редагую, публікую або видаляю.

### 2. Надіслати sitemap у Google Search Console
Тепер туди входить і блог.
- Search Console → Sitemaps → додати `https://proiav.space/sitemap.xml`.

### 3. Перевірити, що в Vercel лежать НОВІ ключі
Після ротації засвічених ключів — переконатися, що в Vercel → Settings →
Environment Variables стоять оновлені значення (B2, Monobank). Якщо міняла
ключі, але не оновила у Vercel — списання/сховище відваляться.

---

## 🟡 CDN через Bunny.net (щоб фото відкривались миттєво)
Код уже вміє віддавати прев'ю/обкладинки через CDN — треба лише ввімкнути
змінною `MEDIA_CDN_URL`. Поки її нема, усе працює як раніше (presigned з B2).

1. **Зробити B2-бакет публічним для читання:** Backblaze → Buckets → мій
   бакет → Bucket Settings → **Files in Bucket are: Public**.
   (Оригінали й далі захищені — публічні лінки на них короткоживучі й видаються
   лише через застосунок; CDN кешує тільки прев'ю/тумби/постери/обкладинки/лого.)
2. **Створити Pull Zone у Bunny:** [bunny.net](https://bunny.net) → CDN →
   Add Pull Zone.
   - Name: `proiav`
   - Origin URL: `https://s3.<B2_REGION>.backblazeb2.com/<B2_BUCKET>`
     (мій регіон і назву бакета взяти з Vercel env `B2_REGION`, `B2_BUCKET` —
     напр. `https://s3.eu-central-003.backblazeb2.com/proyav-media`).
3. **Перевірити:** взяти будь-який ключ файлу і відкрити
   `https://proiav.b-cdn.net/<key>` — має віддати картинку.
4. *(Опційно, гарніше)* Свій піддомен: DNS → CNAME `cdn` → `proiav.b-cdn.net`,
   у Bunny → Pull Zone → Hostnames додати `cdn.proiav.space` і ввімкнути SSL.
5. **Увімкнути:** Vercel → Environment Variables → `MEDIA_CDN_URL` =
   `https://proiav.b-cdn.net` (або `https://cdn.proiav.space`) → **Redeploy**.

---

## 🟡 Емейл-розсилка (завтрашнє завдання)
Щоб клієнти отримували лист «фото готові», а я — сповіщення про броні.

1. Акаунт на [resend.com](https://resend.com) → **верифікувати домен**
   (додати DNS-записи, які Resend покаже: SPF, DKIM).
2. Vercel env: `RESEND_API_KEY` і `EMAIL_FROM`
   (напр. `проЯв <hello@proiav.space>`).
3. Supabase → Authentication → Emails → **Custom SMTP** — щоб листи
   підтвердження пошти теж ішли з мого домену, а не з дефолтного Supabase.

---

## 🔴 Потребує платного акаунта

### Leaked-password protection (Supabase)
Захист від паролів зі злитих баз. Вмикається одним тумблером, але лише
на платному плані.
- Коли перейду на Pro: Supabase → Authentication → Policies →
  **Enable leaked password protection**.

---

## ⚪ Опційно / на потім

- **Портфоліо + демо-галерея.** Завантажити мої роботи й одну показову
  галерею в B2 — щоб на сайті було що показати новим фотографам.
- **РНОКПП у договорі.** Наразі свідомо не публікуємо (в офері написано
  «надаються в рахунку та за запитом»). Якщо колись захочу — додається в
  `src/lib/legal/copy.ts`.
- **Польща 🇵🇱.** Пакет виходу на польський ринок підготовлений і чекає —
  запускаємо, коли Україна повністю налаштована.

---

### Що вже зроблено і НЕ потребує моїх дій
Галереї зі стилями, конструктор сайтів, бронювання з оплатами, тарифи з
grace-періодом, юридичні сторінки (оферта + політика з даними ФОП),
сторінка статистики для засновника, реферальна система, онбординг,
відео-постери, закрита діра з password_hash (перевірено), блог із 6
статтями + движок + адмінка, CDN-готовий код. Усі PR злиті в main.
